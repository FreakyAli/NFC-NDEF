<?xml version="1.0" encoding="utf-8" ?>
<page:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:page="clr-namespace:NDEFMAUI.Views"
             xmlns:vm="clr-namespace:NDEFMAUI.ViewModels"
             x:DataType="vm:MainViewModel"
             Title="Messenger"
             x:Class="NDEFMAUI.MainPage">
    <ContentPage.Content>
        <VerticalStackLayout VerticalOptions="Center">
            <StackLayout
                Margin="10"
                Padding="1"
                BackgroundColor="LightGray">
                <Editor
                    AutoSize="TextChanges"
                    HeightRequest="250"
                    BackgroundColor="White"
                    Placeholder="Write something you would like to share..."
                    MaxLength="250"/>
            </StackLayout>
            <Button Margin="15,0,15,0"
                    BackgroundColor="Blue"
                    TextColor="White"
                    Text="Send Text"/>
        </VerticalStackLayout>
    </ContentPage.Content>
</page:BaseContentPage>

